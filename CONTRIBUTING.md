# 参与贡献

请先阅读 [开发文档](docs/DEVELOPMENT.md)。问题反馈和功能建议可以通过仓库的 Issues 提交；改动尽量围绕一个具体问题。

提交 Pull Request 前：

1. 修改 `src/` 或启动器，避免直接编辑生成 HTML。
2. 执行构建、语法检查、音频/Worker 测试与 Python 测试。
3. 将最新 `dist/index.html` 一同提交。
4. 涉及界面或音频行为时，说明手工验证环境和结果。
5. 同步受影响的文档与变更记录。

不要提交音频素材、凭据、个人电脑绝对路径、依赖缓存或本地日志。测试使用合成信号。引擎的来源、许可和对应源码必须随分发保留。

贡献代码沿用本项目的 GPL-2.0-only 许可；引入其他依赖时，请标注来源及适用许可。
